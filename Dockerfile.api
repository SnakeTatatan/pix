FROM node:24-alpine

RUN apk add --no-cache python3 make g++ git

WORKDIR /app

# Copier tout le monorepo (npm workspaces exige toute l'arborescence)
COPY . .

# Installer toutes les dépendances
RUN npm ci

WORKDIR /app/api
RUN npm install

EXPOSE 3000

CMD ["npm", "start"]
